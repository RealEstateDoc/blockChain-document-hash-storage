{
  "name": "Express-Web3-API",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -L server.js",
    "server": "nodemon -L  server.js",
    "docker:start": "docker-compose -f docker-compose.yml up --build --force-recreate -d",
    "docker:down": "docker-compose -f docker-compose.yml down",
    "docker:logs": "docker-compose -f docker-compose.yml logs -f -t"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ganache-cli": "^6.12.2",
    "nodemon": "^1.18.3"
  },
  "dependencies": {
    "config": "^3.3.8",
    "dotenv": "^8.1.0",
    "ethereumjs-tx": "^1.3.7",
    "express": "^4.16.3",
    "solc": "^0.4.23",
    "web3": "^0.20.2"
  }
}
